<Window x:Class="FarmaciaApp.UI.Views.AgregarEditarProductoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Producto" Height="380" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nombre" Grid.Row="0"/>
        <TextBox Grid.Row="0" Margin="0,22,0,0"
                 Text="{Binding Form.ProNombre, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Text="Precio" Grid.Row="1" Margin="0,10,0,0"/>
        <TextBox Grid.Row="1" Margin="0,32,0,0"
                 Text="{Binding Form.ProPrecio, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Text="Stock" Grid.Row="2" Margin="0,10,0,0"/>
        <TextBox Grid.Row="2" Margin="0,32,0,0"
                 Text="{Binding Form.ProStock, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Descripcion" Grid.Row="3" Margin="0,10,0,0"/>
        <TextBox Grid.Row="3" Margin="0,32,0,0" AcceptsReturn="True" Height="100"
                 Text="{Binding Form.ProDescripcion, UpdateSourceTrigger=PropertyChanged}" />

        <!-- CONTENEDOR CORREGIDO -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Guardar" Width="100" Margin="5" Command="{Binding SaveCommand}" IsEnabled="{Binding CanSave}" />
                <Button Content="Cancelar" Width="100" Margin="5" Command="{Binding CancelCommand}" />
            </StackPanel>

            <TextBlock Grid.Column="1" Foreground="Red" VerticalAlignment="Center" 
                       Margin="10,0,0,0" Text="{Binding ErrorMessage}" />
        </Grid>

    </Grid>
</Window>
